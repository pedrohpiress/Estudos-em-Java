package com.example.ApiAlpha.repository;

import com.example.ApiAlpha.model.LogRequisicao;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogRequisicaoRepository extends JpaRepository<LogRequisicao, Long> {
}

